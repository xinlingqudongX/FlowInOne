---
phase: 04-node-edit-panel
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - frontend/src/nodes/node-card.css
  - frontend/src/config/logicflow.config.ts
  - frontend/src/components/WorkflowEditor.vue
autonomous: true
requirements:
  - EDITOR-01
  - EDITOR-02
  - EDITOR-03
  - EDITOR-04
  - EDITOR-05

must_haves:
  truths:
    - "所有非 RootNode 节点类型（text/image/audio/video/file/property）在 WorkflowEditor 中以 CardNodeView+CardNodeModel 注册，覆盖原有 RectNode 注册"
    - "WorkflowEditor 的 node:click 处理器调用 model.setProperties({expanded: !current}) 切换展开态"
    - "node-card.css 存在并被 WorkflowEditor.vue 导入，提供卡片容器、表单字段、attributes 表格、保存状态提示的基础样式"
    - "前端构建无 TypeScript 类型错误（pnpm run type-check 在 frontend/ 通过）"
    - "所有既有测试仍为 GREEN"
  artifacts:
    - path: "frontend/src/nodes/node-card.css"
      provides: "节点卡片样式"
    - path: "frontend/src/config/logicflow.config.ts"
      provides: "registerCustomNodes 函数更新，HtmlNode 替换 RectNode"
    - path: "frontend/src/components/WorkflowEditor.vue"
      provides: "node:click 展开/收起逻辑"
  key_links:
    - from: "frontend/src/config/logicflow.config.ts"
      to: "frontend/src/nodes/NodeCardRenderer.ts"
      via: "import CardNodeView"
      pattern: "import.*CardNodeView"
    - from: "frontend/src/components/WorkflowEditor.vue"
      to: "frontend/src/nodes/node-card.css"
      via: "import"
      pattern: "import.*node-card.css"
    - from: "frontend/src/components/WorkflowEditor.vue"
      to: "lf.on('node:click')"
      via: "setupLogicFlowEvents"
      pattern: "node:click.*setProperties.*expanded"
---

<objective>
将 CardNodeView/CardNodeModel 接入 LogicFlow 实例：更新节点注册、node:click 展开逻辑、添加卡片 CSS 样式，完成画布集成。

Purpose: Wave 1 的实现体已就绪，本计划将其接入现有 WorkflowEditor 生命周期，使功能在浏览器中可见。
Output: 3 个修改文件，前端构建无错误，pnpm run test 全部 GREEN。
</objective>

<execution_context>
@C:/Users/scp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-node-edit-panel/04-CONTEXT.md
@.planning/phases/04-node-edit-panel/04-RESEARCH.md
@.planning/phases/04-node-edit-panel/04-02-SUMMARY.md

<interfaces>
<!-- 需要修改的现有文件的关键入口点 -->

From frontend/src/config/logicflow.config.ts（当前状态）:
  - createLogicFlowInstance(container: HTMLElement): LogicFlow — 创建 LF 实例，应用主题
  - applyTheme(lf: LogicFlow): void
  - getNodeStyle(nodeType: string): any
  注意：当前无 registerCustomNodes 函数，节点注册逻辑在 WorkflowEditor.vue 中

From frontend/src/components/WorkflowEditor.vue（当前状态）:
  - registerCustomNodes(lf) — 第 554 行附近，注册 RootNode 等类型
  - setupLogicFlowEvents(lf) — 含 lf.on('node:click', ...) 目前只打 console.log
  - 初始化顺序：createLogicFlowInstance → registerCustomNodes → ensureRootNode → lf.render()

From frontend/src/nodes/NodeCardRenderer.ts（Wave 1 产出）:
```typescript
export class CardNodeView extends HtmlNode { ... }
```

From frontend/src/nodes/NodeCardModel.ts（Wave 1 产出）:
```typescript
export class CardNodeModel extends HtmlNodeModel { ... }
export const COLLAPSED_HEIGHT = 80;
export const EXPANDED_HEIGHT_BASE = 300;
```

节点类型列表（需替换为 HtmlNode 的类型）:
  'text' | 'image' | 'audio' | 'video' | 'file' | 'property'
  注意：'root'/'RootNode' 保留原有 RectNode（只展示标题，无编辑需求）
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 node-card.css 并更新节点注册</name>
  <files>frontend/src/nodes/node-card.css, frontend/src/config/logicflow.config.ts</files>
  <action>
    1. 创建 frontend/src/nodes/node-card.css，提供以下选择器的基础样式：

       .node-card
         border: 2px solid #2e73b2; border-radius: 8px; background: #fff;
         font-family: inherit; font-size: 13px; overflow: hidden;
         min-width: 260px; cursor: default; user-select: none;

       .node-card__status-border
         height: 4px; background: #ccc; /* Phase 5 会更新颜色 */

       .node-card__header
         padding: 8px 10px 4px; font-weight: 600; color: #1f5d98;
         display: flex; align-items: center; gap: 6px;

       .node-card__summary
         padding: 4px 10px 8px; color: #555; font-size: 12px;
         white-space: nowrap; overflow: hidden; text-overflow: ellipsis;

       .node-card__edit-form
         padding: 8px 10px; border-top: 1px solid #e8e8e8;
         display: flex; flex-direction: column; gap: 8px;

       .node-card__edit-form label
         font-size: 11px; color: #888; font-weight: 600; text-transform: uppercase;

       .node-card__edit-form textarea
         width: 100%; border: 1px solid #ddd; border-radius: 4px;
         padding: 6px; font-size: 12px; resize: vertical; font-family: inherit;
         box-sizing: border-box;

       .node-card__attributes
         width: 100%; border-collapse: collapse; font-size: 12px;

       .node-card__attributes th, .node-card__attributes td
         border: 1px solid #eee; padding: 4px 6px;

       .node-card__attributes input
         border: none; outline: none; width: 100%; font-size: 12px;

       .node-card__attr-add-btn
         background: #f0f7ff; border: 1px dashed #2e73b2; border-radius: 4px;
         cursor: pointer; padding: 4px 8px; font-size: 12px; color: #2e73b2;
         width: 100%; margin-top: 4px;

       .node-card__save-status
         font-size: 11px; padding: 2px 6px; border-radius: 3px;

       .node-card__save-status--success
         color: #4caf50; background: #f0fff4;

       .node-card__save-status--error
         color: #f44336; background: #fff5f5;

    2. 修改 frontend/src/config/logicflow.config.ts：
       - 在文件顶部添加 import { CardNodeView } from '../nodes/NodeCardRenderer'
       - 在文件顶部添加 import { CardNodeModel } from '../nodes/NodeCardModel'
       - 在 createLogicFlowInstance 函数内，applyTheme(lf) 之前，调用新增的 registerCardNodes(lf)
       - 新增 export function registerCardNodes(lf: LogicFlow): void，内容：
         ```
         const CARD_NODE_TYPES = ['text', 'image', 'audio', 'video', 'file', 'property'];
         for (const type of CARD_NODE_TYPES) {
           lf.register({ type, view: CardNodeView, model: CardNodeModel });
         }
         ```
       - 保留所有现有代码，只做追加修改

    注意：注册必须在 lf.render() 之前。createLogicFlowInstance 返回 lf 实例，WorkflowEditor.vue 调用 render() 是后续步骤，时序正确。
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test 2>&1 | tail -10</automated>
  </verify>
  <done>node-card.css 存在于 frontend/src/nodes/；logicflow.config.ts 含 registerCardNodes 函数和 import；pnpm run test 全部 GREEN</done>
</task>

<task type="auto">
  <name>Task 2: 更新 WorkflowEditor.vue — 引入 CSS 和 node:click 展开逻辑</name>
  <files>frontend/src/components/WorkflowEditor.vue</files>
  <action>
    在 WorkflowEditor.vue 中进行以下修改（精确定位，不重写整个文件）：

    1. 在 `<script setup lang="ts">` 中已有的 import 区域末尾添加：
       ```typescript
       import '../nodes/node-card.css';
       ```

    2. 找到 setupLogicFlowEvents 函数内的 lf.on('node:click', ...) 处理器，当前只有 console.log。
       替换该 handler 的回调体为：
       ```typescript
       lf.on('node:click', ({ data }) => {
         // 跳过 RootNode，不展开编辑区
         if (data.type === 'root' || data.type === 'RootNode') return;
         const model = lf.getNodeModelById(data.id);
         if (!model) return;
         const current = model.getProperties();
         model.setProperties({ expanded: !current.expanded });
       });
       ```
       注意：保留原有 console.log（调试用）或删除，但必须保留整个 lf.on 块的其他逻辑（如果有的话）。

    3. 确保 WorkflowEditor.vue 中 registerCustomNodes 函数内不再手动注册 text/image/audio/video/file/property 类型的 RectNode（如果存在的话），避免覆盖 logicflow.config.ts 中新注册的 CardNode。
       如果 registerCustomNodes 中有上述 6 种类型的 lf.register 调用，删除或注释这些行。
       RootNode 的 lf.register 调用保留不变。

    不修改 createLogicFlowInstance 调用、render 调用、保存逻辑、协作逻辑等其他部分。
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test 2>&1 | tail -10</automated>
  </verify>
  <done>WorkflowEditor.vue 含 import node-card.css 语句；node:click 处理器调用 setProperties({expanded}); pnpm run test 全部 GREEN</done>
</task>

</tasks>

<verification>
cd D:/project/FlowInOne/frontend && pnpm run test 2>&1 | tail -15

预期：全部测试（13 Wave 0 + logicflow-converter）GREEN，无编译错误。
</verification>

<success_criteria>
- frontend/src/nodes/node-card.css 存在，含全部所需选择器
- logicflow.config.ts 导入并调用 registerCardNodes，注册 6 种卡片类型
- WorkflowEditor.vue 的 node:click 处理器切换 expanded property
- pnpm run test（frontend/）全部 GREEN
- 文件已 git commit
</success_criteria>

<output>
完成后创建 .planning/phases/04-node-edit-panel/04-03-SUMMARY.md
</output>
