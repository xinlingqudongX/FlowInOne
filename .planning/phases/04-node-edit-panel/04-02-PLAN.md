---
phase: 04-node-edit-panel
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - frontend/src/nodes/NodeCardRenderer.ts
  - frontend/src/nodes/NodeCardModel.ts
  - frontend/src/services/node-api.service.ts
autonomous: true
requirements:
  - EDITOR-01
  - EDITOR-02
  - EDITOR-03
  - EDITOR-04
  - EDITOR-05

must_haves:
  truths:
    - "CardNodeModel.setAttributes 在 expanded=false 时设 height=80，在 expanded=true 时设 height>=300"
    - "CardNodeView.setHtml 在展开时渲染 requirement textarea、prompt textarea 和 attributes 键值对表格"
    - "增减 attributes 行立即调用 model.setProperties 回写，不依赖 DOM 状态"
    - "patchNode 发出正确的 PATCH fetch 请求，成功/失败后调用 showSaveStatus 更新 DOM 提示"
    - "node-card.spec.ts 和 node-api.spec.ts 全部测试转为 GREEN"
  artifacts:
    - path: "frontend/src/nodes/NodeCardRenderer.ts"
      provides: "CardNodeView (HtmlNode 子类) + CardNodeModel (HtmlNodeModel 子类)"
      exports: ["CardNodeView", "CardNodeModel"]
    - path: "frontend/src/nodes/NodeCardModel.ts"
      provides: "CardNodeModel 独立导出（供 logicflow.config.ts 注册使用）"
      exports: ["CardNodeModel", "COLLAPSED_HEIGHT", "EXPANDED_HEIGHT_BASE"]
    - path: "frontend/src/services/node-api.service.ts"
      provides: "patchNode(nodeId, payload) + showSaveStatus(el, status)"
      exports: ["patchNode", "showSaveStatus"]
  key_links:
    - from: "frontend/src/nodes/NodeCardRenderer.ts"
      to: "frontend/src/services/node-api.service.ts"
      via: "import patchNode"
      pattern: "import.*patchNode.*node-api"
    - from: "frontend/src/nodes/NodeCardRenderer.ts"
      to: "@logicflow/core HtmlNode/HtmlNodeModel"
      via: "extends"
      pattern: "extends HtmlNode"
---

<objective>
实现 CardNodeModel、CardNodeView 和 NodeApiService：让节点卡片能展开/收起内联编辑区，自动保存到后端 PATCH API。

Purpose: 将 Wave 0 RED 测试逐一转为 GREEN，建立可被 WorkflowEditor.vue 注册使用的 HtmlNode 实现。
Output: 3 个 TypeScript 源文件，所有 13 个 Wave 0 测试通过（GREEN）。
</objective>

<execution_context>
@C:/Users/scp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-node-edit-panel/04-CONTEXT.md
@.planning/phases/04-node-edit-panel/04-RESEARCH.md
@.planning/phases/04-node-edit-panel/04-01-SUMMARY.md

<interfaces>
<!-- 已有契约，实现必须满足 -->

From @logicflow/core (已在 package.json):
```typescript
import { HtmlNode, HtmlNodeModel } from '@logicflow/core';

class HtmlNodeModel {
  width: number;
  height: number;  // observable setter，赋值触发 Preact 重渲染
  properties: Record<string, any>;
  setAttributes(): void;  // properties 变化时自动调用
  setProperties(props: Record<string, any>): void;
  getProperties(): Record<string, any>;
}

class HtmlNode {
  props: { model: HtmlNodeModel };
  setHtml(rootEl: SVGForeignObjectElement): void;  // 渲染 HTML 内容
  shouldUpdate(): boolean;  // 返回 false 则跳过 setHtml
}
```

From frontend/src/types/logicflow.types.ts:
```typescript
export type NodeStatus = 'pending' | 'completed' | 'failed' | 'review_needed';
export interface ExtendedNodeConfig {
  properties?: {
    title?: string;
    requirement?: string;
    prompt?: string | null;
    attributes?: Array<{ key: string; value: string }>;
    expanded?: boolean;
    nodeType?: string;
    [key: string]: any;
  };
}
```

PATCH API 契约 (Phase 2 已实现):
  PATCH /api/v1/node/:id
  Body: { requirement?: string, prompt?: string, attributes?: Array<{key:string,value:string}> }
  Success: 200 JSON
  Error: 4xx/5xx JSON { message?: string }

Wave 0 测试文件（实现需使这些测试 GREEN）:
  frontend/src/tests/node-card.spec.ts  — 8 个测试
  frontend/src/tests/node-api.spec.ts   — 5 个测试
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现 node-api.service.ts（EDITOR-05 GREEN）</name>
  <files>frontend/src/services/node-api.service.ts</files>
  <behavior>
    - patchNode('node-1', {requirement:'foo'}) 发出 PATCH /api/v1/node/node-1，Content-Type: application/json
    - patchNode 成功时返回 res.json()
    - patchNode HTTP 非 2xx 时抛出 Error，message 含 HTTP 状态码
    - showSaveStatus(el, 'success') 在 el 内写入包含"已保存"的 span，2s 后清除
    - showSaveStatus(el, 'error') 在 el 内写入包含"保存失败"的 span，持续显示（不自动清除）
  </behavior>
  <action>
    创建 frontend/src/services/node-api.service.ts：

    1. 导出 async function patchNode(nodeId: string, payload: { requirement?: string; prompt?: string; attributes?: Array<{key:string;value:string}> }): Promise<unknown>
       - fetch(`/api/v1/node/${nodeId}`, { method:'PATCH', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) })
       - if (!res.ok) { const err = await res.json().catch(() => ({})); throw new Error(err.message ?? `HTTP ${res.status}`); }
       - return res.json()

    2. 导出 function showSaveStatus(containerEl: Element, status: 'success' | 'error'): void
       - 查找或创建 containerEl 内 .node-card__save-status span
       - status==='success': textContent='已保存 ✓'，className='node-card__save-status node-card__save-status--success'，setTimeout 2000ms 后清空 textContent
       - status==='error': textContent='保存失败'，className='node-card__save-status node-card__save-status--error'（不自动清除）

    3. 导出 function debounce<T extends (...args: any[]) => any>(fn: T, ms: number): (...args: Parameters<T>) => void
       - 标准 clearTimeout + setTimeout 实现（约 10 行）
       - 供 NodeCardRenderer.ts 使用

    不引入任何新依赖，使用原生 fetch。
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test -- --reporter=verbose node-api 2>&1 | tail -20</automated>
  </verify>
  <done>node-api.spec.ts 的 5 个测试全部 GREEN；无 TypeScript 编译错误（pnpm run test 不报类型错误）</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现 NodeCardModel.ts 和 NodeCardRenderer.ts（EDITOR-01..04 GREEN）</name>
  <files>frontend/src/nodes/NodeCardModel.ts, frontend/src/nodes/NodeCardRenderer.ts</files>
  <behavior>
    - CardNodeModel.setAttributes: expanded=false → height=80; expanded=true → height 根据 attributes 行数动态计算（最小 300）
    - CardNodeView.setHtml: 展开时渲染 .node-card__field--requirement textarea + .node-card__field--prompt textarea + .node-card__attributes table
    - CardNodeView.setHtml: 收起时渲染 .node-card__summary 显示 requirement 首 50 字符或"未填写"
    - attributes 增行：onclick 读取 model.properties.attributes，append {key:'',value:''}，调用 model.setProperties({attributes:[...]})
    - attributes 删行：onclick 读取 index，filter 掉对应行，调用 model.setProperties({attributes:[...]})
    - textarea oninput：500ms 防抖后调用 patchNode + showSaveStatus
    - 卡片 mousedown/click 事件调用 stopPropagation（防止触发 LogicFlow node:click）
    - shouldUpdate() 始终返回 true（确保 setProperties 后卡片内容重绘）
  </behavior>
  <action>
    1. 创建 frontend/src/nodes/NodeCardModel.ts：
       ```typescript
       import { HtmlNodeModel } from '@logicflow/core';

       export const COLLAPSED_HEIGHT = 80;
       export const EXPANDED_HEIGHT_BASE = 300;

       export class CardNodeModel extends HtmlNodeModel {
         setAttributes() {
           this.width = 280;
           if (this.properties.expanded) {
             const attrCount = (this.properties.attributes ?? []).length;
             this.height = EXPANDED_HEIGHT_BASE + attrCount * 36;
           } else {
             this.height = COLLAPSED_HEIGHT;
           }
         }
       }
       ```

    2. 创建 frontend/src/nodes/NodeCardRenderer.ts：
       - import { HtmlNode } from '@logicflow/core'
       - import { CardNodeModel } from './NodeCardModel'
       - import { patchNode, showSaveStatus, debounce } from '../services/node-api.service'

       CardNodeView 实现：
       - shouldUpdate(): 始终返回 true（覆盖基类比较逻辑）
       - setHtml(rootEl): rootEl.innerHTML = ''; 构建卡片 DOM
         a. 创建 div.node-card，阻止 mousedown/click 冒泡
         b. 顶部 div.node-card__header：显示 nodeType 图标文字 + 类型名
         c. 中部 div.node-card__summary：显示 (props.requirement ?? '').slice(0,50) || '未填写'
         d. 预留 div.node-card__status-border（Phase 5 颜色逻辑占位，空 div）
         e. 若 props.expanded：追加 buildEditForm(model, rootEl) 结果
         f. rootEl.appendChild(card)

       buildEditForm(model, rootEl) 内部函数（不导出）：
       - 创建 section.node-card__edit-form
       - requirement 字段：label "需求说明" + textarea.node-card__field--requirement（rows=4，value=props.requirement??''）
         oninput: debouncedSave（500ms，取当前 textarea.value，调用 patchNode(model.id, {requirement:val})，成功/失败调用 showSaveStatus(card, ...)）
       - prompt 字段：label "AI 提示词" + textarea.node-card__field--prompt（rows=6，value=props.prompt??''）
         oninput: 同上，保存 prompt 字段
       - attributes 字段：label "属性" + table.node-card__attributes
         表格头：key | value | 操作
         每行：input[key] | input[value] | 删除按钮（onclick: 删除该行 → setProperties）
         表格底部：+ 添加行 按钮（onclick: 增加空行 → setProperties）
       - 防抖函数：每个节点的 debounced save 在闭包内创建，捕获 model 引用

       重要：buildEditForm 中的防抖 save 函数读取 model.properties 获取 nodeId（model.id），payload 仅含变更字段

    3. 两个文件均导出对应类，供 logicflow.config.ts 导入注册

    按照 Wave 0 测试期望的类名和 CSS 类名实现，不能改变测试中已写定的选择器：
    - .node-card__field--requirement
    - .node-card__field--prompt
    - .node-card__attributes
    - .node-card__summary
    - .node-card__save-status
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test -- --reporter=verbose node-card 2>&1 | tail -30</automated>
  </verify>
  <done>node-card.spec.ts 的 8 个测试全部 GREEN；node-api.spec.ts 仍 GREEN；无 TypeScript 类型错误</done>
</task>

</tasks>

<verification>
cd D:/project/FlowInOne/frontend && pnpm run test 2>&1 | tail -20

预期：全部 13 个 Wave 0 测试（8 + 5）GREEN，logicflow-converter.spec.ts GREEN。
</verification>

<success_criteria>
- frontend/src/nodes/NodeCardRenderer.ts 存在，导出 CardNodeView
- frontend/src/nodes/NodeCardModel.ts 存在，导出 CardNodeModel
- frontend/src/services/node-api.service.ts 存在，导出 patchNode + showSaveStatus + debounce
- pnpm run test（frontend/）全部 13 个测试 GREEN
- 文件已 git commit
</success_criteria>

<output>
完成后创建 .planning/phases/04-node-edit-panel/04-02-SUMMARY.md
</output>
