---
phase: 04-node-edit-panel
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/src/tests/node-card.spec.ts
  - frontend/src/tests/node-api.spec.ts
autonomous: true
requirements:
  - EDITOR-01
  - EDITOR-02
  - EDITOR-03
  - EDITOR-04
  - EDITOR-05

must_haves:
  truths:
    - "node-card.spec.ts 存在并包含 RED 测试，覆盖 EDITOR-01..04 的 CardNodeModel/CardNodeView 行为"
    - "node-api.spec.ts 存在并包含 RED 测试，覆盖 EDITOR-05 的 patchNode fetch 调用和反馈 DOM"
    - "pnpm run test (frontend/) 全部测试运行，Wave 0 测试用例失败（RED），其余既有测试仍为 GREEN"
  artifacts:
    - path: "frontend/src/tests/node-card.spec.ts"
      provides: "CardNodeModel + CardNodeView 行为测试桩（RED）"
      exports: []
    - path: "frontend/src/tests/node-api.spec.ts"
      provides: "patchNode 服务测试桩（RED）"
      exports: []
  key_links:
    - from: "frontend/src/tests/node-card.spec.ts"
      to: "frontend/src/nodes/NodeCardRenderer.ts"
      via: "import"
      pattern: "import.*NodeCardRenderer"
    - from: "frontend/src/tests/node-api.spec.ts"
      to: "frontend/src/services/node-api.service.ts"
      via: "import"
      pattern: "import.*node-api"
---

<objective>
建立 Wave 0 测试脚手架：为 Phase 4 的所有 5 个需求写 RED 测试桩，确保测试框架覆盖后续实现。

Purpose: 保证 Nyquist 合规——实现前先有失败测试，波次推进有明确验收基线。
Output: node-card.spec.ts（EDITOR-01..04）、node-api.spec.ts（EDITOR-05），两份测试均以 RED 状态提交。
</objective>

<execution_context>
@C:/Users/scp/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/scp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-node-edit-panel/04-CONTEXT.md
@.planning/phases/04-node-edit-panel/04-RESEARCH.md

<interfaces>
<!-- 测试将引用的目标模块（尚未创建，测试为 RED 的原因） -->

目标路径（Wave 1 将创建）:
  frontend/src/nodes/NodeCardRenderer.ts  — CardNodeView (HtmlNode 子类), CardNodeModel (HtmlNodeModel 子类)
  frontend/src/services/node-api.service.ts — patchNode(nodeId, payload) 函数

已有类型（可在测试中引用）:
From frontend/src/types/logicflow.types.ts:
```typescript
export type NodeStatus = 'pending' | 'completed' | 'failed' | 'review_needed';
export interface ExtendedNodeConfig {
  properties?: {
    requirement?: string;
    prompt?: string | null;
    attributes?: Array<{ key: string; value: string }>;
    expanded?: boolean;
    [key: string]: any;
  };
}
```

已有测试基础设施:
  frontend/src/tests/setup.ts    — Vitest setup 文件，jsdom 环境已配置
  frontend/vite.config.ts        — test.environment = 'jsdom', setupFiles 已引用 setup.ts
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 node-card.spec.ts（EDITOR-01..04 RED 测试桩）</name>
  <files>frontend/src/tests/node-card.spec.ts</files>
  <behavior>
    - Test 1 (EDITOR-01): CardNodeModel.setAttributes — expanded=false 时 height=80，expanded=true 时 height>=300
    - Test 2 (EDITOR-01): node:click 切换 expanded — 调用 model.setProperties({expanded: true}) 后 model.properties.expanded 为 true
    - Test 3 (EDITOR-02): CardNodeView.setHtml — expanded=true 时 rootEl 内存在 .node-card__field--requirement textarea
    - Test 4 (EDITOR-03): CardNodeView.setHtml — expanded=true 时 rootEl 内存在 .node-card__field--prompt textarea
    - Test 5 (EDITOR-04): CardNodeView.setHtml — expanded=true 时 rootEl 内存在 .node-card__attributes 表格行
    - Test 6 (EDITOR-04): 增行按钮点击后 model.properties.attributes 数组长度加 1
    - Test 7 (EDITOR-01): CardNodeView.setHtml — collapsed 时 rootEl 显示 requirement 首行截断（最多 50 字符）
    - Test 8 (EDITOR-01): CardNodeView.setHtml — requirement 为空时显示"未填写"占位文字
  </behavior>
  <action>
    创建 frontend/src/tests/node-card.spec.ts，内容如下：

    1. import { describe, it, expect, vi, beforeEach } from 'vitest'
    2. 尝试 import { CardNodeView, CardNodeModel } from '../nodes/NodeCardRenderer' — 因目标文件不存在，vi.mock 或直接 import 将失败，使整个 spec 为 RED
    3. 为避免 import 报错导致无法运行其他测试，使用 vi.mock('../nodes/NodeCardRenderer', () => ({})) 并在每个 it 块内 expect(CardNodeView).toBeDefined() / expect(CardNodeModel).toBeDefined() 来产生 RED
    4. 覆盖全部 8 个 behavior 用例，每个 it 块内断言对应行为，目前均应失败
    5. 注意：测试使用 jsdom 环境，document.createElement 可用；mock LogicFlow model 用普通对象

    具体结构：
    ```
    describe('CardNodeModel', () => {
      it('collapsed height=80', ...)
      it('expanded height>=300', ...)
    })
    describe('CardNodeView', () => {
      it('renders requirement textarea when expanded', ...)
      it('renders prompt textarea when expanded', ...)
      it('renders attributes table when expanded', ...)
      it('add row updates model.properties.attributes', ...)
      it('shows truncated requirement in summary', ...)
      it('shows 未填写 when requirement empty', ...)
    })
    ```
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test -- --reporter=verbose node-card 2>&1 | head -40</automated>
  </verify>
  <done>node-card.spec.ts 文件存在；pnpm run test 运行时 node-card 相关测试失败（RED）且输出可见测试名称；logicflow-converter.spec.ts 仍为 GREEN</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 创建 node-api.spec.ts（EDITOR-05 RED 测试桩）</name>
  <files>frontend/src/tests/node-api.spec.ts</files>
  <behavior>
    - Test 1 (EDITOR-05): patchNode('node-1', {requirement:'foo'}) — 发出 PATCH /api/v1/node/node-1，Content-Type: application/json，body 含 requirement
    - Test 2 (EDITOR-05): patchNode 成功时返回解析后的 JSON
    - Test 3 (EDITOR-05): patchNode 失败时（HTTP 500）抛出 Error，message 含 HTTP 状态码
    - Test 4 (EDITOR-05): showSaveStatus(el, 'success') — 在 el 内写入包含"已保存"文字的元素
    - Test 5 (EDITOR-05): showSaveStatus(el, 'error') — 在 el 内写入包含"保存失败"文字的元素
  </behavior>
  <action>
    创建 frontend/src/tests/node-api.spec.ts：

    1. vi.mock('../services/node-api.service', () => ({})) — 同样使目标 import 为 RED
    2. 使用 vi.stubGlobal('fetch', vi.fn()) 模拟 fetch
    3. 在各 it 块内断言 patchNode / showSaveStatus 的行为，目前均失败
    4. 不引入额外依赖，使用 Vitest 内置 mock 能力

    具体结构：
    ```
    describe('patchNode', () => {
      it('sends PATCH to /api/v1/node/:id', ...)
      it('returns parsed JSON on success', ...)
      it('throws on HTTP error', ...)
    })
    describe('showSaveStatus', () => {
      it('shows 已保存 on success', ...)
      it('shows 保存失败 on error', ...)
    })
    ```
  </action>
  <verify>
    <automated>cd D:/project/FlowInOne/frontend && pnpm run test -- --reporter=verbose node-api 2>&1 | head -40</automated>
  </verify>
  <done>node-api.spec.ts 文件存在；node-api 相关测试以 RED 状态运行；整体 `pnpm run test` 有 RED（Wave 0 桩）+ GREEN（既有测试）混合输出</done>
</task>

</tasks>

<verification>
cd D:/project/FlowInOne/frontend && pnpm run test 2>&1 | tail -20

预期：logicflow-converter 测试 GREEN，node-card + node-api 测试 RED（因目标模块不存在）。
不允许：既有 GREEN 测试变 RED，类型错误导致 setup.ts 崩溃。
</verification>

<success_criteria>
- frontend/src/tests/node-card.spec.ts 存在，含 8 个 it 块
- frontend/src/tests/node-api.spec.ts 存在，含 5 个 it 块
- `pnpm run test`（frontend/）：Wave 0 测试 RED，logicflow-converter.spec.ts 仍 GREEN
- 文件已 git commit
</success_criteria>

<output>
完成后创建 .planning/phases/04-node-edit-panel/04-01-SUMMARY.md
</output>
